"use client";
{% raw %}
import Link from "next/link";
import { ChatWidget } from "@/components/iblai/chat-widget";
import { config } from "@/lib/config";

export default function Home() {
  return (
    <div className="relative" style={{ width: "100vw", height: "100vh" }}>
      {/* Profile settings link */}
      <Link
        href="/profile"
        className="absolute top-4 right-4 z-50 flex items-center justify-center rounded-full bg-white/80 p-2 shadow-md backdrop-blur-sm hover:bg-white transition-colors"
        title="Profile Settings"
      >
        <svg
          width="20"
          height="20"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
          <circle cx="12" cy="7" r="4" />
        </svg>
      </Link>

      <ChatWidget
        mentorId={config.defaultAgentId()}
        width="100vw"
        height="100vh"
      />
    </div>
  );
}
{% endraw %}
